.panel
  .panel-body
    .row
      - @data.each_slice(4) do |d|
        - (0..3).each do |i|
          - if @link = d[i]
            .col-md-4
              %a{ href: "https://ru.wikipedia.org#{@link[:url]}" }= @link[:text]
